services:
  backend:
    build: ./backend
    expose:
      - "8080"

  nginx:
    image: nginx:latest
    ports:
      - "1337:1337"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
